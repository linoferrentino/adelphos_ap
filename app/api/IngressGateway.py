# this is the ingress gateway.

# this is called synchronously.

class IngressGateway:

    
    # here I have the application context.


    # this is the entry point for the post request, the only way to access
    # adelphos
    @staticmethod
    def process_new_request(request: Request) -> int:
        pass

