[project]
name = "adelphos-ap"
version = "0.1.0"
description = "Adelphos, the fractal trust network. Activity Pub implementation"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "aiohttp[speedups]>=3.13.3",
    "anyio>=4.12.0",
    "argon2-cffi>=25.1.0",
    "cryptography>=46.0.3",
    "fastapi[standard]>=0.125.0",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "websockets>=15.0.1",
]
